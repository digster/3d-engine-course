# 2026-09-12 — The builder reproducibility repair

The task 6.14 filed, picked up. **All 38 page generators now reproduce their published page
byte-identically**, verified by a new standing check, `docs/_template/check-builders.py`, which is
wired into CLAUDE.md §11, README §12/§15 and ARCHITECTURE.md.

Starting state, confirmed independently by the new harness before anything was touched — it
reproduced the audit's failure sets exactly: **11 CRASH** (37, 38, 39, 42–49), **16 DIFF** (51–511,
61, 62, 64, 66, 67), 11 ok.

## The harness, and the measurement that was wrong before

The audit that missed this asked ONE question — "did the page file change?" — of a THREE-axis
situation: did it exit 0, did it write anything, does what it wrote match. A crashing builder writes
nothing, so eleven crashes scored as eleven passes. `check-builders.py` reports the three separately
and never infers one from another. It removes the page from the clone *first*, so a crash shows as
EMPTY rather than as the inherited copy.

Each builder runs in a **copy-on-write clone** (`cp -Rc`, APFS `clonefile`): 100 MB in ~0.1 s, so the
whole 38-builder sweep takes **10 seconds** and the working tree is never touched. `--figures` (42 s,
keeps `build/`) also regenerates the SVGs first.

## The repair, and what made it mechanical

**The rebuild diff is the oracle** — every line it prints is a correction living on the page and not
in its source. The whole job became: derive pairs from the diff, route each to whichever source
produced that line, re-run. The routing is the interesting part, because one page line has four
possible spellings:

| spelling | source |
|---|---|
| verbatim | a body fragment |
| HTML-escaped | a pinned code listing (through `esc()`) |
| re-indented | an inline SVG (the builder indents it) |
| **not a line at all** | a figure caption wrapped across Python string literals |

Tools written: `pin_listings.py` (freeze + *verify each pin against the page*), `which_commit.py`,
`fix_builder.py`, `autoport.py`, `extract_listing.py`, `extend_pins.py`.

## Findings worth carrying

- **A published page is a lossless archive of its own sources.** §8's "zero placeholders" means every
  listing is embedded whole, so when `scratch/verify_54.cpp` — gitignored, no history — turned out to
  have been rewritten by 6.4 and 6.7, its 5.4-era text was recoverable **from the page**. Six
  listings recovered this way (5.4, 5.6, 6.1, 6.2, 6.6 ×4, 6.7).

- **`html.unescape()` is not the inverse of a five-rule `esc()`.** It decodes the whole HTML5 entity
  table. Caught *only* because the extractor asserts its own round-trip.

- **Which commit to pin is decidable, not a judgement call.** The page embeds each listing escaped,
  so it is a substring test. Two pages needed a LATER lesson's commit (below) and guessing would have
  silently rewritten them.

- **The drift class recurses.** Page ← SVG ← `figs_NN.py`. Fixing only the SVG leaves the generator
  able to revert it. Found in 5.7; `--figures` is the check.

- **My own tool shipped the bug it exists to catch.** `port_line.py` treated TSV lines starting with
  `#` as comments; two of 5.1's corrections are CMake comment lines, silently dropped while the run
  reported success. Fixed by deleting the comment convention — it had no user and only a failure
  mode. Same family as `class="tag mod"`.

- **A correction can sit at any offset in a wrapped Python string.** 6.6's changed character was the
  FIRST character of a continuation line, so every fragment with leading context spanned a line
  break; 5.7's SVG label needed ZERO trailing context because the common suffix was `.</text>`.
  `autoport` now searches with context on either side and neither.

## Two pedagogical defects found, deliberately NOT fixed

Cause A committed to history. Correcting them changes a published lesson's **content**, which is the
author's call, not a reproducibility fix. Flagged in LEARNINGS.md, STATE.md and the builders' own
comments.

- **05-02 shows Lesson 5.3's code** (+156/-38 from `ea7a05f`): `ENGINE_LOG_*` where 5.2 wrote
  `SDL_Log`, `image_report` where 5.2 wrote `image_status`, and `#include <engine/core/log.hpp>` — a
  header 5.3 creates. Violates §8's "every listing compiles at this point in the course".
- **05-04 shows Lesson 5.5's code** (`d599928`), including a doc comment reading "Lesson 5.5 replaced
  this struct's contents" — past tense, inside Lesson 5.4.

Both happened the same way: the next lesson's session re-ran the previous builder to retrofit a
`next` nav link, and the live reads pulled the new code in. These two are the **only** pages where
that commit changed more than the nav links — the other thirteen were `+2/-2`, which is what makes
the damage bounded and findable.

## One page byte-changed, zero rendered characters

`06-06-gltf.html`: two raw `'` inside a **generated** listing, hand-typed by the 7756e93 retrofit
where `esc()` emits `&#x27;`. No pin can make `esc()` produce a bare apostrophe, so the page was
normalised to what its generator produces. A hand-edit to generated output is a fork; one side has to
give, and the generator wins.

## Verified

`check-builders.py` **38/38** byte-identical. `check-curriculum.py` green (10 modules, 107 lessons,
~510 h). `apply-shared.py --check` clean across 75 pages. `check-page.js` in headless Chromium over
HTTP: **74 pages × 2 widths = 148 checks, 0 failures**. Working tree otherwise clean.

Known gap, recorded not fixed: `figs_45/46/48.py` read `.ppm` captures that later sessions
overwrote, so `--figures` reports those three as DIFF. The published SVGs are correct and the pages
rebuild from them; it is the SVGs' own inputs that are lost.

`next:` unchanged — **6.15 — Skybox and Image-Based Lighting**.

---

# 2026-09-12 (second session) — Lesson 6.15: Skybox and Image-Based Lighting

`next` → **6.15 shipped**. The constant `ambient` the engine has carried since 3.6 is retired in
favour of an environment map, on both renderers. 54 checks, 0 failures; golden byte-identical at
`E917C06C` for the **twenty-fourth** lesson.

## What the lesson is about, in one line each

Two things were wrong with `albedo * ambient` and only one was ever written down: **no direction**,
and **no specular half at all** — the latter on record in a shipped comment since 6.4, naming 6.15
by number. An environment map fixes both, and the same texture is the sky you see and the light you
receive.

- **The cube's geometry is where a correctness bug lives.** `dω = dA/r³`, so a corner texel is
  `1/(3√3) = 0.1925` of a centre one. Equal weights are **1.01% too bright** — and sixteen times the
  texels does not move it (1.010005 → 1.010066). A *bias*, not a discretisation error.
- **Only the diffuse half is exact.** Lambert's BRDF does not depend on `l`, so it leaves the
  integral. Second-order convergence measured, and `image_based_light` reproduces 6.2's
  `albedo × ambient` end to end to **1.291e-05** on a uniform sky. The old term is a special case.
- **The split sum's error has a shape**, measured against brute force at 1e6 samples *against the
  same cube the engine reads*: 0.08% at roughness 0.10, ≤5.8% head-on, **29.0%** at grazing,
  **73.0%** with a sun. The four-fold asymmetry *is* `n = v = r`. The sun case is a third mechanism.
  **The error depends on the environment, not only on the material** — not in the paper.
- **The roughness→mip mapping is derived**, from 6.14's closed-form lobe width, by putting both the
  lobe and the texel in steradians. The folklore is within **0.77 of a level** everywhere, and now
  has a direction: a near-mirror is *over*-blurred.

## The four bugs, and two produced plausible output

1. **ARGB vs RGBA in the BRDF table.** Hand-written shift put `scale` in blue. Green is bits 8–15 in
   both layouts so `bias` read *correctly*, and the shader's `F0*scale + bias` then reduced to `bias`
   alone — ~0 head-on, ~0.078 grazing. Not broken; **dim in a way that invited tuning.** Appeared a
   second time in the harness's unpack. Fix: `pack_argb`, which has been right since 1.6.
2. **An unconverged Monte Carlo reference.** 8,192 samples vs 1,000,000 disagree by **1.98×** with a
   6000:1 sun disc; 0.040% without it. 6.14's rule about null results **has a mirror**: check a
   non-null result has *converged*. Both are one rule — know what your instrument can see first.
3. **A seam test that measured nothing, twice.** First the probes never crossed a face; then they
   did and the answer was *still* zero, because `sky_radiance` depends only on `d.y` and is constant
   along a vertical edge. On an azimuthally-varying probe the seam is **12.09%**. *A null result
   from a fixture that cannot show the defect is a measurement of the fixture.*
4. **A build bug latent for fourteen lessons.** `engine_use_shaders` lacked a target-level edge for
   cross-directory shader files. Nobody hit it because **once a shader has been built once the file
   exists, and `make` will depend on an existing file it has no rule for** — so every earlier shader
   worked from its second build on. A clean tree would have failed on all twenty-three at once.

## check-page.js gained a rect test, and it caught this lesson's own figures

The text-on-shape check selected `line, polyline, path`, so an annotation box across a label was
invisible. Figures 1, 5 and 6 all shipped first drafts with exactly that. The new check samples the
four **edges** so "crosses" stays distinct from "contains", and is restricted to `rect[fill="none"]`
— a restriction *measured*, not assumed: over every rect it fires 26 times on 12 published pages,
nearly all labels deliberately annotating filled cells. Hollow boxes only: 9 hits on 4 pages, left
alone deliberately (same call as this morning's two pedagogical defects).

## Verified

`check-builders.py` **39/39** byte-identical, including the new `build_615.py`.
`check-curriculum.py` green (10 modules, 107 lessons, ~510 h, 71 published).
`check-page.js` in real Chromium over HTTP: **pass at 1280 and 390, in system, light and dark**.
Rebuilding `build_614.py` after repointing its nav gave exactly the two lines meant to move —
**fourteenth lesson running**.

`next:` → **6.16 — Frustum Culling and Instanced Submission**, with the warning that its golden
argument is *different in kind*: culling changes which draws happen, so a moved golden would be a
bug rather than a re-baseline.

---

# Session 2 — Lesson 6.16, Frustum Culling and Instanced Submission

*(Same date, merged into this file rather than a `-b` suffix.)*

## Pinned 6.15 first, as `next:` instructed

`pin_listings.py 615` wrote five pins (four from `5c54785`, plus the gitignored `verify_615.cpp`
from the working tree) and verified all five by substring against the shipped page. `build_615.py`
then reproduced `06-15-skybox-ibl.html` **byte for byte** — md5 `ef8c1b95…` before and after.

## Part 1: the culler

`engine/gfx/frustum.{hpp,cpp}`. Planes from the **rows of `clip_from_world`**, derived from the one
observation that a clip coordinate is already a signed distance. `bounds.hpp` gained `sphere`,
`bounds_of` and `bounding_sphere`, redeeming the promise 6.8 wrote into its own header.

Things that went wrong and were worth it:

- **The sign on the wrong row.** `row0 − row3` instead of `row3 − row0` rejects the whole scene, and
  the tell is that opposite planes report *exactly negated* distances. `verify_616` §A asserts on it.
- **Four zeros are not enough.** The eye is the apex, so its distance to all four side planes is
  exactly 0 — but a *reversed* normal passes all four too, which is precisely the bug made. Checks 2
  and 4 exist for that.
- **The far plane is 1.4e-3 out of place and it is not the extraction's fault.** The obvious
  diagnosis is cancellation in `row3 − row2`. Redoing the subtraction in `double` from the same
  `float` matrix reproduced the answer to seven digits, which *rules it out*. It is upstream, in
  `perspective()`: `A + 1` with `A = −1.003009` drops eight bits and amplifies one ulp by **332×**.

## Part 2: instancing

`engine/gfx/instancing.{hpp,cpp}`, `shaders/scene_instanced.vert.hlsl`, a tenth pipeline and
`gpu_scene_renderer::render_batched`. `render()`'s ninety-line prologue became a private
`begin_frame` rather than being copied — the split is exactly at "does this change within a frame?".

**The bytes do not move; the calls do.** `gpu_instance` is 112 bytes, exactly `sizeof(object_uniforms)`.
On 16 cubes: 3152 B / 16 draws → 640 B + 1792 instance B / **one** draw. The whole 720-byte saving
is fifteen *materials* not pushed.

## The measurement that nearly wasn't

First §H swept the **object count** and reported "cull wins" six times — meaningless, because both
the test and the work it skips are O(n) so their ratio cannot depend on n. Rewritten around the
**cull rate**, with the break-even *predicted* as `c/w` = 58.3 / 23,821 = **0.2446%** (one object in
409) and then bracketed between the 0% and 0.4% samples. At 0% culled it loses by 53.7 ns/object,
which is exactly `c`.

## Three findings that are not about culling

1. `pipeline_desc` capped vertex attributes at **8** and silently dropped the rest. Never reached in
   fifteen lessons; the instanced pipeline wants eleven. Caught by 4.5's `check_layout` reading the
   shader's reflection. Cap now 16, and it asserts.
2. `gpu_scene_renderer::destroy()` had never been taught about 6.15's `black_cube_`, `black_lut_`
   and `cube_sampler_`. Not a leak, but "destroyed" and "empty" had stopped meaning the same thing.
   Found by *adding a member and reading the list*.
3. `golden_615.cpp` printed `identical=YES` when **both** file reads failed — two empty strings are
   equal. Always run from the right directory, so it never fired. `golden_616.cpp` reports sizes and
   a differing-byte count.

Also: `aabb::expand({1,2,3})` is ambiguous (brace elision reaches both overloads), and
`std::vector<int> v(std::size_t(n))` is the most vexing parse. Both are in the lesson's pitfalls.

## The golden is blind to this lesson, and says so

14 objects across the reference shot's 8 frames, **0 culled** — nothing is ever off screen, so the
unchanged hash proves nothing. The instrument was then checked: the same code culls 1 when an object
is moved 200 units away. Part 2 needed a correctness test *built*: `render` vs `render_batched`,
**0 of 76,800 channels differing**, guarded by a lit-pixel count — which caught the first run, where
both images were black because `cube_mesh()` carries no normals.

## Verified

`verify_616` **60 checks, 0 failures** (47 CPU-only, 13 needing a GPU; driver `metal`).
Golden byte-identical at `E917C06C`, **twenty-fifth** lesson.
`check-builders.py` **40/40**. `check-curriculum.py` green (72 published — it caught the stale hero
stat). `check-page.js` **pass at 1280 and 390**. A **clean-tree** build from scratch succeeds, which
is 6.15's build-bug finding acted on. `-Wall -Wextra` clean.

`next:` → **6.17 — A Frame Graph**, with the warning that `gpu_scene.*` is unusually likely to move,
since `render_batched` is a new path through the scene pass that landed one lesson before the graph
arrived to describe it.
